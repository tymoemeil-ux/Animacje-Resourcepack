// ============================================================
// EFEKT (kolor: krystal (HYBRYDA)): KRYSTAL   |   kolor spustowy: #60C004
// krystal
// ============================================================
if (c == ivec3(96, 192, 4)) {
        tfxIDg = 172.0;

    tfxOffg.y += (tfxHash(vec2(g, 3.0)) - 0.5) * 0.004 + sin(GameTime * 2.0 + g) * 0.003;
}
