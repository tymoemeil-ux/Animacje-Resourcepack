// ============================================================
// EFEKT (kolor: zachod (HYBRYDA)): ZACHOD   |   kolor spustowy: #60C038
// zachod
// ============================================================
if (c == ivec3(96, 192, 56)) {
        tfxIDg = 185.0;

    tfxOffg.x += sin(GameTime * 0.9 + g * 0.4) * 0.004;
}
