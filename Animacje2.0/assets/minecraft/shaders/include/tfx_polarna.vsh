// ============================================================
// EFEKT (kolor: polarna (HYBRYDA)): POLARNA   |   kolor spustowy: #60C008
// polarna
// ============================================================
if (c == ivec3(96, 192, 8)) {
        tfxIDg = 173.0;

    tfxOffg.y += sin(GameTime * 0.9 + g * 0.4) * 0.005;
}
