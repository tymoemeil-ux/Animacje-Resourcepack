// ============================================================
// EFEKT: STATIC TV   |   kolor spustowy: #A0A064
// szum telewizyjny — losowy szum pikselowy co klatke
// ============================================================
if (c == ivec3(160, 160, 100)) {
        tfxIDg = 38.0;
        tfxBaseg = vec4(1.0);
}
