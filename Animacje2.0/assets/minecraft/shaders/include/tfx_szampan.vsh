// ============================================================
// EFEKT (kolor: szampan (HYBRYDA)): SZAMPAN   |   kolor spustowy: #60C018
// szampan
// ============================================================
if (c == ivec3(96, 192, 24)) {
        tfxIDg = 177.0;

    tfxOffg.y += (tfxHash(vec2(floor(GameTime * 6.0), g)) - 0.5) * 0.005;
}
